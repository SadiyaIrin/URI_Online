#include<bits/stdc++.h> 
using namespace std; 

int main()
{
  double A,B,Avg = 0;
  cin >>fixed >> setprecision(1) >> A >> B;
  Avg = ((A * 3.5) + (B * 7.5)) / (3.5 + 7.5);
  cout <<fixed << setprecision(5) << "MEDIA = "<< Avg<<endl;

  return 0;
}

#include<bits/stdc++.h> 
using namespace std; 

int main()
{
  double A,B,Avg;
  cin >> A >> B >> fixed >> setprecision(1);
  Avg = ((A*3.5) + (B*7.5)) / (3.5 + 7.5);
  cout << "MEDIA = " << Avg << fixed << setprecision(4) << endl;

  return 0;
}

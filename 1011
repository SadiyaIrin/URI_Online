#include <bits/stdc++.h>
using namespace std;

int main() {
  double R,Pi = 3.14159,Sphere_Area;
  cin >> R;
  
  Sphere_Area = (4.0/3) * Pi * R * R * R;

  cout << fixed << setprecision(3)<< "VOLUME = "<< Sphere_Area <<"\n";

return 0;
}
